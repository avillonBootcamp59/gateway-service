spring:
  application:
    name: ms-gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t
      fail-fast: true
      retry:
        max-attempts: 5
        initial-interval: 1000

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
